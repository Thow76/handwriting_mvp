// Scoring tests — cleared for incremental rebuild.
//
// Step 1: Procrustes shape scoring (position/scale/rotation independence)
// Step 2: Bitmap IoU completeness
// Step 3: Placement scoring
// Step 4: Proportion / Size scoring
// Step 5: Combined score + difficulty levels

void main() {
  // Tests will be added incrementally as each scoring component is built.
}
